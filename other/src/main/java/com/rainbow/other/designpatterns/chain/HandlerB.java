package com.rainbow.other.designpatterns.chain;

public class HandlerB implements IHandler {

    @Override
    public boolean handle() {
        boolean handled = false;
        // ....
        return handled;
    }
}
